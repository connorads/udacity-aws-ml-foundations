from setuptools import setup

setup(name='connorads-gaussian-package-example',
      version='1.0',
      description='An example Python package that does Gaussian distribution stuff',
      packages=['distributions'],
      zip_safe=False)